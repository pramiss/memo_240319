com.memo.config 패키지
WebMvcConfig 클래스 : 서버의 images 폴더와 WAS의 연결

@Configuration // 설정을 위한 Spring bean (마찬가지로 component 기능을 가짐)
// --> FileManagerService에서 따로 WebMvcConfig 클래스를 호출할 필요 없이
자동으로 매핑됨




사진 이름 (Jinshi.jpg) 에서 확장자 추출
let extension = fileName.split(".").pop();
[1] 대신 pop()을 쓰는 이유는 pop()은 확실히 마지막 거를 가져오기 때문.
보통 파일 이름에 .은 없으므로 두 조각만 나서 [1]로 가져와도 되지만
좀 더 확실히 하려면 pop()이 더 나은 방식