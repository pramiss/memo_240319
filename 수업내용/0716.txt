MEMO - 글 목록
SNS - 타임라인 글 목록

* 글 목록에서 로그인 여부 확인
Integer userId = (Integer)session.getAttribute("userId");
=> 1. getAttr()의 리턴 타입은 오브젝트여서 형변환을 해줘야함
=> 2. int로 받으면 null 검사를 불가능!!

* MyBatis로 도메인 작성 시 어노테이션
@ToString
@Data // lombok으로 자동 getter/setter